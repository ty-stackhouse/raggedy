[project]
name = "rag-app"
version = "0.1.0"
description = "RAG Application with GitHub Integration"
requires-python = ">=3.11,<3.12"
package-mode = false 

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
